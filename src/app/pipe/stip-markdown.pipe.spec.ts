import { StipMarkdownPipe } from './stip-markdown.pipe';

describe('StipMarkdownPipe', () => {
  it('create an instance', () => {
    const pipe = new StipMarkdownPipe();
    expect(pipe).toBeTruthy();
  });
});
